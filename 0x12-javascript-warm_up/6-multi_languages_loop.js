#!/usr/bin/node
// Printing an elements of an array using a loop
const myLoveLangs = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let elements = 0; elements < 3; elements++) {
  console.log('%s', myLoveLangs[elements]);
}
